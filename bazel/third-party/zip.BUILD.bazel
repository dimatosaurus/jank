load("@rules_cc//cc:cc_library.bzl", "cc_library")

package(default_visibility = ["//visibility:public"])

cc_library(
    name = "zip",
    srcs = ["src/zip.c"],
    hdrs = glob(["src/*.h"]),
    includes = ["src"],
    copts = ["-w"],
)
